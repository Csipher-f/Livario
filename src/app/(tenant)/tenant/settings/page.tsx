import { ModulePage } from "@/components/app-shell/ModulePage";

export default function TenantSettingsPage() {
  return (
    <ModulePage
      description="Manage profile preferences, notification settings, security, and account details."
      eyebrow="Tenant workspace"
      title="Settings"
    />
  );
}
