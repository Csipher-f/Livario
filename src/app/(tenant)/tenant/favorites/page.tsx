import { ModulePage } from "@/components/app-shell/ModulePage";

export default function FavoritesPage() {
  return (
    <ModulePage
      description="Keep promising listings, saved searches, and viewing notes in one place."
      eyebrow="Tenant workspace"
      title="Favorites"
    />
  );
}
